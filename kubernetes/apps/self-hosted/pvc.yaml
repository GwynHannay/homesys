---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: local-self-hosted-volume
  namespace: self-hosted
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-storage